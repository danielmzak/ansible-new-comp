---
- hosts: laptop
  become: true  # Use sudo for tasks that require it
  become_method: sudo
  roles:
    - common
    - apt_sudo_packages
- hosts: laptop
  roles:
    - apt_packages
    - snap_packages
    - flatpak_packages
    - custom_installs
